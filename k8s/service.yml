apiVersion: v1
kind: Service
metadata:
  name: goproxy
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "goproxy"
    port: 8080
  selector:
    app: goproxy
